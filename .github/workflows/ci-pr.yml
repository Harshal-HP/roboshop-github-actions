name: CI - PR
on:
  workflow_call:
    inputs:
      appType:
        required: true
        type: string
jobs:
  Build:
    uses: Harshal-HP/roboshop-github-actions/.github/workflows/Build.yml@main
    with:
      appType: '${{ inputs.appType }}'
  Test:
    runs-on: self-hosted
    needs: Build
    steps:
      - run: echo OK